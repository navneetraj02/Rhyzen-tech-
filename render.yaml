services:
  - type: web
    name: rhygen
    env: static
    envVars:
      - key: PNPM_VERSION
        value: 10.33.2
    buildCommand: pnpm install && pnpm run build
    staticPublishPath: artifacts/rhygen/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
